# iOS Dopamine 越狱网速状态栏插件实现方案

## 1. 项目目标

开发一个兼容 Dopamine rootless 越狱环境的 iOS tweak，在系统状态栏区域实时显示设备的下载和上传速度。

目标显示效果：

```text
↓ 1.2M/s  ↑ 320K/s
```

第一版建议支持：

- iOS 15+
- Dopamine rootless
- SpringBoard 注入
- Wi-Fi 与蜂窝网络总流量
- 每秒刷新一次
- 自动切换 B/s、K/s、M/s
- 竖屏优先，后续适配横屏和锁屏

## 2. 技术路线

```text
Theos/Logos
    │
    └── 注入 com.apple.springboard
            │
            ├── 创建透明 UIWindow
            │       └── 显示上传/下载 UILabel
            │
            └── 定时读取网络接口累计字节数
                    └── 计算单位时间内的字节差
```

### 推荐方案：自定义状态栏悬浮视图

在 SpringBoard 中创建透明、不可交互的窗口，并把它放在状态栏窗口层级上方。这个方案比直接 Hook iOS 私有状态栏组件更容易兼容多个系统版本。

视觉上可以接近原生状态栏：

- 使用系统字体
- 使用等宽数字字体避免数字变化时抖动
- 使用 SF Symbols 的上下箭头
- 自动适配深色和浅色模式
- 根据屏幕尺寸和状态栏高度动态定位

### 不建议第一版直接接入私有状态栏布局

真正把网速注册成系统状态栏 item，需要逆向 SpringBoard 的私有状态栏组件。这样可以获得最原生的排列和动画效果，但 iOS 15、16、17 的内部类和布局逻辑可能不同，维护成本较高。

## 3. Dopamine 兼容要求

Dopamine 使用 rootless 架构，插件必须使用 rootless 方式编译和打包。

关键要求：

- `THEOS_PACKAGE_SCHEME=rootless`
- 最低部署版本建议为 iOS 15.0
- 包架构为 `iphoneos-arm64`
- 编译架构使用 `arm64 arm64e`
- Filter 只注入 `com.apple.springboard`
- 不要在代码中写死传统 rootful 路径
- 安装后需要重启 SpringBoard
- Dopamine 设置中需要启用 Tweak Injection

参考：

- [Theos Rootless 文档](https://theos.dev/docs/rootless)
- [Theos 打包文档](https://theos.dev/docs/packaging)
- [Dopamine 官方仓库](https://github.com/opa334/Dopamine)

## 4. Theos 项目配置

### Makefile

```makefile
ARCHS = arm64 arm64e
TARGET = iphone:clang:latest:15.0

include $(THEOS)/makefiles/common.mk

TWEAK_NAME = NetSpeedStatus

NetSpeedStatus_FILES = Tweak.xm
NetSpeedStatus_FRAMEWORKS = UIKit
NetSpeedStatus_CFLAGS = -fobjc-arc

include $(THEOS_MAKE_PATH)/tweak.mk

after-install::
	install.exec "killall -9 SpringBoard"
```

### NetSpeedStatus.plist

```plist
{
    Filter = {
        Bundles = (
            "com.apple.springboard"
        );
    };
}
```

### control

```text
Package: com.example.netspeedstatus
Name: NetSpeedStatus
Version: 1.0.0
Architecture: iphoneos-arm64
Description: Display real-time upload and download speed in the status bar.
Maintainer: Your Name
Author: Your Name
Section: Tweaks
Depends: firmware (>= 15.0)
```

## 5. 网速统计实现

使用 `getifaddrs()` 遍历网络接口，并从 `struct if_data` 读取：

- `ifi_ibytes`：累计接收字节数
- `ifi_obytes`：累计发送字节数

基本公式：

```text
下载速度 = (本次接收总字节数 - 上次接收总字节数) / 时间间隔
上传速度 = (本次发送总字节数 - 上次发送总字节数) / 时间间隔
```

建议：

- 排除 `IFF_LOOPBACK` 回环接口
- 默认统计 `en0` 和 `pdp_ip*`
- 根据实际设备测试 VPN、个人热点和 USB 网络接口
- 使用 1 秒采样周期
- 采用指数平滑减少数字跳动

示例接口过滤逻辑：

```objc
NSString *name = [NSString stringWithUTF8String:ifa->ifa_name];

BOOL supported = [name isEqualToString:@"en0"] ||
                 [name hasPrefix:@"pdp_ip"];

if (!supported) {
    continue;
}
```

## 6. 状态栏视图实现

窗口初始化建议：

```objc
netWindow = [[UIWindow alloc] initWithFrame:UIScreen.mainScreen.bounds];
netWindow.windowLevel = UIWindowLevelStatusBar + 1;
netWindow.backgroundColor = UIColor.clearColor;
netWindow.userInteractionEnabled = NO;
netWindow.hidden = NO;
```

标签建议：

```objc
label.font = [UIFont monospacedDigitSystemFontOfSize:9.0
                                               weight:UIFontWeightMedium];
label.textColor = UIColor.whiteColor;
label.adjustsFontSizeToFitWidth = YES;
```

不要固定写死顶部坐标。应根据以下因素重新布局：

- `safeAreaInsets`
- 状态栏高度
- 屏幕宽度
- 刘海和 Dynamic Island
- 横竖屏变化
- 锁屏和控制中心显示状态

## 7. SpringBoard 注入入口

```objc
%hook SpringBoard

- (void)applicationDidFinishLaunching:(UIApplication *)application {
    %orig;

    dispatch_after(
        dispatch_time(DISPATCH_TIME_NOW, 2 * NSEC_PER_SEC),
        dispatch_get_main_queue(), ^{
            CreateNetSpeedWindow();
        }
    );
}

%end
```

延迟创建窗口是为了避免 SpringBoard 启动早期窗口层级尚未稳定。

## 8. 更新策略

建议使用主线程更新 UI，网络接口读取可以放在后台队列：

```text
后台队列读取接口数据
        │
        └── 计算速度
                │
                └── 主线程更新 UILabel
```

不要在主线程执行阻塞式网络操作，也不要每几十毫秒刷新一次。1 秒刷新已经足够实时，并且更省电。

建议的平滑算法：

```objc
smoothDownload = smoothDownload * 0.7 + currentDownload * 0.3;
smoothUpload = smoothUpload * 0.7 + currentUpload * 0.3;
```

## 9. 原生状态栏效果的取舍

### 自定义窗口方案

优点：

- 跨 iOS 版本相对稳定
- 实现简单
- 不依赖大量私有类
- 适合 Dopamine rootless

缺点：

- 不是系统真正的状态栏 item
- 可能被锁屏、控制中心或其他插件覆盖
- 需要自己处理横屏、刘海和 Dynamic Island

### 私有状态栏 Hook 方案

优点：

- 可以和电池、信号图标共享布局
- 原生状态栏动画和颜色更自然
- 更接近系统效果

缺点：

- 依赖 iOS 私有 API
- 需要按系统版本适配
- 更容易因为系统更新导致崩溃
- 调试和维护成本较高

项目建议先采用自定义窗口方案，待 iOS 版本和设备范围确定后，再考虑私有状态栏集成。

## 10. 设置项设计

后续可以增加 PreferenceLoader 设置页面：

- 总开关
- 显示下载速度
- 显示上传速度
- 显示位置
- 字体大小
- 颜色
- 显示单位：B/s、bit/s、自动
- 刷新间隔
- 是否统计 VPN 接口
- 是否显示在锁屏

设置变更后发送 Darwin 通知，让 SpringBoard 重新加载配置，无需重启设备。

## 11. 测试清单

### 基础功能

- [ ] 插件成功注入 SpringBoard
- [ ] Wi-Fi 下载速度正确
- [ ] Wi-Fi 上传速度正确
- [ ] 蜂窝网络速度正确
- [ ] 无网络时显示 0K/s
- [ ] 网络切换后仍能正常统计

### Dopamine 环境

- [ ] 包架构为 `iphoneos-arm64`
- [ ] 使用 rootless 打包
- [ ] 安装路径位于 `/var/jb`
- [ ] 重启 SpringBoard 后自动加载
- [ ] 重新越狱后插件仍然生效
- [ ] 关闭 Tweak Injection 后插件不再加载

### UI 兼容性

- [ ] 无刘海设备
- [ ] 刘海设备
- [ ] Dynamic Island 设备
- [ ] 深色模式
- [ ] 浅色模式
- [ ] 横屏
- [ ] 锁屏
- [ ] 控制中心
- [ ] 电话、热点和 CarPlay 状态

### 稳定性

- [ ] 连续运行 24 小时无 SpringBoard 崩溃
- [ ] 无明显耗电增加
- [ ] 无内存持续增长
- [ ] 与常见状态栏插件共存
- [ ] 异常接口数据不会造成整数溢出

## 12. 构建和安装命令

```bash
THEOS_PACKAGE_SCHEME=rootless make clean package
```

设备安装：

```bash
dpkg -i ./packages/com.example.netspeedstatus_1.0.0_iphoneos-arm64.deb
killall -9 SpringBoard
```

查看注入日志：

```bash
log stream --predicate 'eventMessage CONTAINS "NetSpeedStatus"'
```

## 13. 推荐的第一版范围

第一版不要立即实现真正的私有原生状态栏 item，建议先完成：

1. SpringBoard 注入。
2. rootless `.deb` 打包。
3. Wi-Fi 和蜂窝网速统计。
4. 透明窗口显示上下行速度。
5. 深色模式和刘海设备适配。
6. 连续运行稳定性测试。

等基础版本稳定后，再针对具体设备和 iOS 版本研究私有状态栏布局。
